using VU_SRA_ASP.NET.Models;
using Microsoft.EntityFrameworkCore;

namespace VU_SRA_ASP.NET.Data
{
    public class StudentContext : DbContext
    {
        public StudentContext(DbContextOptions options) :base(options)
        {

        }
        public Data 
    }
}